# =============================================================================
# FixReviews - Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local or any file containing secrets

# -----------------------------------------------------------------------------
# Application
# -----------------------------------------------------------------------------
NEXT_PUBLIC_APP_NAME=FixReviews
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_DEFAULT_LOCALE=fr

# Sign-in enablement
NEXT_PUBLIC_SIGNIN_ENABLED=true

# -----------------------------------------------------------------------------
# Database
# -----------------------------------------------------------------------------
# PostgreSQL connection string
# Example: postgresql://user:password@localhost:5432/fixreviews
DATABASE_URL=<your_postgres_connection_string>

# -----------------------------------------------------------------------------
# Authentication (NextAuth.js)
# -----------------------------------------------------------------------------
# Generate a random secret: openssl rand -base64 32
NEXTAUTH_SECRET=<your_nextauth_secret>
NEXTAUTH_URL=http://localhost:3000

# Google OAuth
# Get credentials from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=<your_google_client_id>
GOOGLE_CLIENT_SECRET=<your_google_client_secret>

# -----------------------------------------------------------------------------
# Payment Processing (Stripe)
# -----------------------------------------------------------------------------
# Stripe Checkout links (direct payment links, not embedded)
# Create these in your Stripe Dashboard under Products
STRIPE_CHECKOUT_PRO_URL=<https://buy.stripe.com/your_pro_link>
STRIPE_CHECKOUT_PREMIUM_URL=<https://buy.stripe.com/your_premium_link>

# Optional: Make checkout URLs public (if needed on client-side)
NEXT_PUBLIC_STRIPE_CHECKOUT_PRO_URL=<https://buy.stripe.com/your_pro_link>
NEXT_PUBLIC_STRIPE_CHECKOUT_PREMIUM_URL=<https://buy.stripe.com/your_premium_link>

# -----------------------------------------------------------------------------
# AI Providers (Optional - for review response generation)
# -----------------------------------------------------------------------------
# OpenAI API Key
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=<your_openai_api_key>

# Anthropic (Claude) API Key
# Get from: https://console.anthropic.com/
ANTHROPIC_API_KEY=<your_anthropic_api_key>

# -----------------------------------------------------------------------------
# Email (for notifications and magic links)
# -----------------------------------------------------------------------------
# AWS SES credentials (if using SES)
AWS_ACCESS_KEY_ID=<your_aws_access_key>
AWS_SECRET_ACCESS_KEY=<your_aws_secret_key>
AWS_REGION=eu-west-1

# Or use another email provider (SMTP, SendGrid, etc.)
# Configure in src/lib/email/sendMail.ts

# -----------------------------------------------------------------------------
# Development & Testing
# -----------------------------------------------------------------------------
# Set to 'development' or 'production'
NODE_ENV=development

# =============================================================================
# SECURITY REMINDER
# =============================================================================
# ⚠️  NEVER commit this file with real values
# ⚠️  NEVER share your secrets publicly
# ⚠️  Keep .env.local in your .gitignore
# ⚠️  Rotate secrets regularly
# =============================================================================
